# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Local .app bundle produced by run.sh
build/

# Xcode
*.xcodeproj/
*.xcworkspace/
DerivedData/
*.xcuserstate
xcuserdata/

# macOS
.DS_Store

# Never commit anything captured from the operator's live Claude Code data.
# Transcripts and session files are private work product (Bamboo.md §6).
*.jsonl
scratch/
